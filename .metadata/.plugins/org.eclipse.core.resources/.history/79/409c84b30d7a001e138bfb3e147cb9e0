package com.elm.sdp.service;

import java.util.List;

import com.elm.project.models.Employee;
import com.elm.project.models.LeaveForm;

public interface EmployeeService
{
	public String addemployee(com.elm.sdp.model.Employee emp);
	void sendConfirmationEmail(Employee emp);
	public Employee checkemplogin(String email,String pwd);
	public String addForm(LeaveForm form);
	public List<Employee> viewallreqs(LeaveForm form);
	void sendEmail(LeaveForm emp);
}
